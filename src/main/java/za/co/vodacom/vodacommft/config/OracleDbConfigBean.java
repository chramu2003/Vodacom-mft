package za.co.vodacom.vodacommft.config;

/*

@Component
@Configuration
@PropertySource(value={"file:${app.HOME}/si_globals/si_global.properties",
        "file:${app.SI_INSTALL}/properties/sandbox.cfg",
        "file:${app.SI_INSTALL}/properties/jdbc_customer.properties"})
public class OracleDbConfigBean {

    @Value("${ORACLE_POOL}")
    private String oracle_pool;
    @Value("${VODA_POOL}")
    private String voada_pool;
    @Value("${SALT}")
    private String salt;
    @Value("${DB_URL}")
    private String db_url;
    @Value("${DB_PASSWORD}")
    private String db_password;
    @Value("${vodaPool.user}")
    private String vodaPool_user;
    @Value("${vodaReportPool.user}")
    private String voda_report_pool;

    public String getVodaPool_user() {
        return vodaPool_user;
    }

    public void setVodaPool_user(String vodaPool_user) {
        this.vodaPool_user = vodaPool_user;
    }

    public String getVoda_report_pool() {
        return voda_report_pool;
    }

    public void setVoda_report_pool(String voda_report_pool) {
        this.voda_report_pool = voda_report_pool;
    }

    public String getOracle_pool() {
        return oracle_pool;
    }

    public void setOracle_pool(String oracle_pool) {
        this.oracle_pool = oracle_pool;
    }

    public String getVoada_pool() {
        return voada_pool;
    }

    public void setVoada_pool(String voada_pool) {
        this.voada_pool = voada_pool;
    }

    public String getSalt() {
        return salt;
    }

    public void setSalt(String salt) {
        this.salt = salt;
    }

    public String getDb_url() {
        return db_url;
    }

    public void setDb_url(String db_url) {
        this.db_url = db_url;
    }

    public String getDb_password() {
        return db_password;
    }

    public void setDb_password(String db_password) {
        this.db_password = db_password;
    }


    */
/*@Bean
    public DataSource dataSource() {
        DriverManagerDataSource dataSource = new DriverManagerDataSource();

        dataSource.setDriverClassName("com.oracle.cj.jdbc.Driver");
        dataSource.setUsername("oracle_user from properties");
        dataSource.setPassword("oracle_password from properties");
        dataSource.setUrl(
                "oracle_url from properties...");
        return dataSource;
    }*//*

}

*/
